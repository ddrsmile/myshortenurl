#!/bin/bash

NAME="shorten_url"
WEBAPPDIR=/Users/Joey/webapps/shorten_url/deploy/dev/bin
DJANGODIR=/Users/Joey/webapps/shorten_url/shorten_url
DEPLOYDIR=/Users/Joey/webapps/shorten_url/deploy
SOCKFILE=/Users/Joey/webapps/shorten_url/deploy/run/gunicorn_shorten_url.sock
USER=Joey
GROUP=staff
NUM_WORKERS=3
DJANGO_SETTINGS_MODULE=project.settings.dev
DJANGO_WSGI_MODULE=project.wsgi

echo "String $NAME as `whoami`"

cd $DEPLOYDIR
source env/bin/activate
export DJANGO_SETTINGS_MODULE=$DJANGO_SETTINGS_MODULE
export PYTHONPATH=$DJANGODIR:$PYTHONPATH

RUNDIR=$(dirname $SOCKFILE)
test -d $RUNDIR || mkdir -p $RUNDIR

exec ${DEPLOYDIR}/env/bin/gunicorn ${DJANGO_WSGI_MODULE}:application \
    --name $NAME \
    --workers $NUM_WORKERS \
    --user=$USER  --group=$GROUP \
    --bind=unix:$SOCKFILE \
    --log-level=debug \
    --log-file=-
